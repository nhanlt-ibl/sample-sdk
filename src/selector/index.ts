export const selectAuthen = (state: any) => state.authen.isAuthen;
export const selectAuthenProfile = (state: any) => state.authen.data;
export const selectAuthenState = (state: any) => state.authen;
